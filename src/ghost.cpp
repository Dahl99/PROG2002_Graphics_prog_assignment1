#include "ghost.hpp"

void Ghost::UpdateSprite(const GLint& dir)
{
	/*switch (dir)
	{
	case 0:
		pos.y += dt * mvSpeed;
		break;
	case 1:
		pos.x += dt * mvSpeed;
		break;
	case 2:
		pos.y -= dt * mvSpeed;
		break;
	case 3:
		pos.x -= dt * mvSpeed;
		break;
	default:
		break;
	}*/
}
